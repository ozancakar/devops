apiVersion: apps/v1
kind: Deployment
metadata:
  name: ozan-cakar-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ozan-cakar
  template:
    metadata:
      labels:
        app: ozan-cakar
    spec:
      serviceAccountName: default
      containers:
      - name: ozan-cakar-container
        image: ozanncakar/devops:v2
        ports:
        - containerPort: 80
